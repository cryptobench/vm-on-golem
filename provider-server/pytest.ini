[pytest]
asyncio_mode = auto
# Measure only the core, unit-tested surface for base coverage.
# Use repo-root relative paths to be robust under 'poetry -C'.
addopts = --cov=provider-server/provider/api --cov-report=term-missing --cov-config=.coveragerc
